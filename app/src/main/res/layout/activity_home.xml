<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <!-- Mensagem de boas-vindas -->
    <TextView
        android:id="@+id/tvWelcome"
        android:text="Bem vindo, User!"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnViewTransactions"
        app:layout_constraintVertical_bias="0.3"/>

    <!-- Botão para ver a lista de transações -->
    <Button
        android:id="@+id/btnViewTransactions"
        android:text="Ver transações"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/tvWelcome"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp"
        app:layout_constraintBottom_toTopOf="@+id/btnNewTransaction"/>

    <!-- Botão para criar nova transação -->
    <Button
        android:id="@+id/btnNewTransaction"
        android:text="Adicionar nova transação"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnViewTransactions"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- NOVO BOTÃO: Ver Relatório Financeiro -->
    <Button
        android:id="@+id/btnReport"
        android:text="Resumo Financeiro"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:backgroundTint="#FF9800"
        app:layout_constraintTop_toBottomOf="@id/btnNewTransaction"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>